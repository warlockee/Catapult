"""
Benchmarker - Standalone Docker container for running model benchmarks.

This package provides a CLI tool that can be run inside a Docker container
to benchmark ML model endpoints and report progress via HTTP callbacks.
"""

__version__ = "1.0.0"
